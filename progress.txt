# Ralph Progress Log
Started: Mon Apr 13 09:07:57 AM PDT 2026
---

## Codebase Patterns
- Migrations: Always use `IF NOT EXISTS`. Apply with `npm run migrations:local` / `npm run migrations:staging`.
- CSS: Compile with `npm run build:css` before deploy. Never edit `generated-styles.ts` directly.
- D1 queries: `env.NEXUS_DB.prepare(...).bind(...).all()` / `.first()` / `.run()`
- KV: Sessions (7-day TTL), magic links (15-min TTL), rate limiting, trace counts
- Auth: `nexus_session` HttpOnly cookie. `requireAuth` middleware in `src/middleware/`.
- SQL escaping in .sql files: Use `''` to escape single quotes (NOT `\'` which is shell syntax)
- subscriptions table: Use `ORDER BY rowid DESC LIMIT 1` for latest sub. Production d1_migrations may lag.
- Demo data: D1 seed at `scripts/seed-demo.sql` (demo-user-seed-001). Route `src/routes/demo.ts` queries D1, falls back to hardcoded DEMO_TRACES/DEMO_SPANS in `src/pages/demo.ts`.
- Smoke test pattern: `hasTestEndpoints` flag gates bootstrap tests. Production runs with SKIP_AUTH_TESTS=1.
- Pre-existing test failures (not my work): keys.spec empty name, mobile webkit missing (webkit not installed on this machine).
- D1 agent health query: use CAST(IFNULL(...COUNT..., 0) AS INTEGER) for subquery count fields — avoids null issues in TypeScript.
- PRD structure: prd.userStories (not prd.stories) — use `prd.userStories.find(s => s.id === ...)`

---

## 2026-04-13 — ACP-160
- What was implemented: Demo seed script for D1 database
- Files changed:
  - `scripts/seed-demo.sql` — idempotent SQL seed with INSERT OR IGNORE; creates demo user, 5 agents, 53 traces across 7 days, 11 spans for representative traces; ~10% error/timeout rate
  - `package.json` — added `seed-demo:local`, `seed-demo:staging`, `seed-demo:prod` npm scripts
- Seed verified locally (53 traces, 5 agents, idempotency confirmed), on staging (268 rows written), on prod (268 rows written)
- No app code changes; smoke tests: 61/63 chromium pass on staging (2 pre-existing failures), 39 pass + 72 skip on prod (no failures from my changes)
- **Learnings for future iterations:**
  - SQL escape single quotes with `''` not `\'` — wrangler d1 execute treats the file as raw SQL, not shell
  - wrangler d1 execute `--file` is the right approach for seeding; `INSERT OR IGNORE` handles idempotency cleanly
  - Must run `npm run migrations:local` before first local seed (tables don't exist in fresh DB)
  - D1 remote seed takes ~30ms on prod vs ~8ms on staging for same data
---

## 2026-04-13 — ACP-161
- What was implemented: Public demo dashboard at /demo loading real D1 seed data
- Files changed:
  - `src/pages/demo.ts` — exported DemoTrace, DemoSpan, DemoAgentCard interfaces; updated demoOverviewPage() and demoTraceDetailPage() to accept optional data params with graceful fallback to hardcoded arrays; h1 "Overview" → "Demo Dashboard"; CTA text → "Start monitoring your agents — free"
  - `src/routes/demo.ts` — full async D1 query of demo-user-seed-001 traces, agent health cards, monthly total; parallel D1 queries; try/catch fallback to hardcoded data
  - `src/pages/landing.ts` — "View demo" → "See a live demo →" (both hero and footer CTA)
  - `test/smoke/public.spec.ts` — added 4 ACP-161 smoke tests: Demo Dashboard header, D1 trace detail (demo-tr-cs-001), landing page CTA text, read-only nav check
  - `src/generated-styles.ts` / `tailwind/output.css` — CSS rebuild
- Smoke tests: 64/65 chromium pass on staging (1 pre-existing keys failure), 29 pass + 36 skip on prod (SKIP_AUTH_TESTS=1, 0 failures from my changes)
- **Learnings for future iterations:**
  - PRD structure uses `prd.userStories` not `prd.stories` — check with `Object.keys(prd)` if unsure
  - D1 subquery COUNT fields should use `CAST(IFNULL(..., 0) AS INTEGER)` to avoid null/string type issues in TypeScript
  - When refactoring page functions to accept optional params, grep for all variable names in the template string — easy to miss old names still referenced
  - The demo route can be async (Hono supports it without any config change) — existing sync demoRoutes.get('/', ...) just becomes async
---

## 2026-04-13 — ACP-162
- What was implemented: Dashboard onboarding widget for new users with zero traces
- Files changed:
  - `src/pages/dashboard.ts` — replaced simple `onboardingChecklist` with rich `onboardingWidget`: 3-step design with masked API key + reveal/copy, tabbed Python/TypeScript/curl code snippets with one-click copy, animated "waiting" state with 5s polling JS; auto-reload with celebration on first trace. `DashboardMetrics` gained `apiKeyPrefix?: string`.
  - `src/index.ts` — dashboard route now fetches `key_prefix` from api_keys; added `GET /dashboard/onboarding/check` JSON endpoint (returns `{ hasTrace: boolean }`) for polling
  - `test/smoke/dashboard.spec.ts` — 6 new tests: widget visible for new user, key prefix displayed, "create key" prompt when no key, widget hidden after first trace, polling endpoint hasTrace false→true, mobile 375px
- Smoke tests: staging 88/142 pass (all chromium tests pass; 54 pre-existing mobile webkit failures). Prod: 41 pass + 84 skip + 17 pre-existing mobile failures (SKIP_AUTH_TESTS=1).
- **Learnings for future iterations:**
  - For Playwright API polling tests, use `page.request.get()` (shares browser context cookies) NOT `request.get()` (standalone fixture, no auth cookies)
  - We only store `key_prefix` (e.g. `nxs_abc123`) not the full plaintext key — onboarding widget shows prefix as masked hint, links to /dashboard/keys for full key
  - Dashboard onboarding widget shows when `!hasTrace && !onboardingDismissed` (changed from old `!hasApiKey && !hasTrace` condition)
  - Tab switching in pure JS: toggle `hidden` class + `bg-indigo-700`/`bg-gray-800` on buttons to avoid needing a JS framework
---

## 2026-04-13 — ACP-163
- What was implemented: Changelog page at /changelog with category badges, recent entries, RSS link, footer/sidebar links
- Files changed:
  - `src/pages/changelog.ts` — updated entry() to support 'feature' | 'improvement' | 'fix' | 'content' types; fixed badge colors (green=Feature, blue=Improvement, orange=Fix, purple=Content); added 20+ entries for Apr 7–13 (waterfall, webhooks, onboarding widget, mobile audit, CD, demo, etc.); added RSS link in header; added /changelog link in footer
  - `src/pages/docs.ts` — added /changelog and /blog links in sidebar TOC; added /changelog link in docs footer
  - `src/pages/landing.ts` — added /changelog link in landing page footer
  - `src/generated-styles.ts` — rebuilt CSS to include orange/purple badge classes
  - `test/smoke/public.spec.ts` — expanded changelog tests: dates, all 4 badge types, RSS link, OG meta tags, sidebar link, footer link, mobile 375px
- Smoke tests: staging 91 pass + 57 pre-existing webkit+keys.spec failures; prod 42 pass + 84 skip + 22 pre-existing webkit failures
- **Learnings for future iterations:**
  - When adding new Tailwind color classes (bg-orange-900, bg-purple-900), must rebuild CSS with `npm run build:css` or they won't appear
  - Changelog page was already scaffolded (imported in index.ts at line 8) — check for existing stubs before creating new files
  - Pre-existing prod smoke test baseline: 42 pass + 84 skip + 22 webkit mobile failures (SKIP_AUTH_TESTS=1)
---

## 2026-04-13 — ACP-164
- What was implemented: API reference documentation at /docs/api-reference
- Files changed:
  - `src/pages/api-reference.ts` — new page with full REST API reference: method badges, POST /api/v1/traces, POST /api/v1/traces/:id/spans, PATCH /api/v1/traces/:id, GET /api/v1/traces/:id, GET /health; request/response schemas, curl examples, error codes table (400–500), rate limit docs, authentication guide, anchor links, mobile responsive layout
  - `src/index.ts` — route registration for GET /docs/api-reference
  - `src/pages/docs.ts` — added /docs/api-reference link in sidebar navigation
  - `test/smoke/public.spec.ts` — 6 new ACP-164 smoke tests: endpoint coverage, error codes table, anchor links, sidebar link, OG meta tags, mobile 375px
- Smoke tests: staging 96 pass + pre-existing webkit failures; prod 48 pass + 84 skip + pre-existing webkit failures (new baseline: 48 pass vs 42 before)
- **Learnings for future iterations:**
  - API reference page was a pure new file — no scaffold stub existed unlike changelog. Import and route both needed adding in index.ts
  - Prod baseline has shifted up: now 48 pass + 84 skip (was 42 + 84 before ACP-164)
---

## 2026-04-13 — ACP-165
- What was implemented: Interactive API playground on /docs/api-reference page
- Files changed:
  - `src/pages/api-reference.ts` — added `#playground` section with API key input (sessionStorage), endpoint selector (GET /health, POST/PATCH traces, GET trace), dynamic trace ID field, pre-filled JSON body, live curl command builder, copy-to-clipboard, Send button (fetch() to real API), response area with status badge + latency + pretty JSON + error hints (401/403/404/429/500). Added sidebar anchor.
  - `test/smoke/public.spec.ts` — 6 new ACP-165 tests: playground section present, sidebar link, curl updates on endpoint change, GET /health send returns 200, sessionStorage persistence, mobile 375px
- Smoke tests: staging 100 pass + pre-existing webkit failures; prod 52 pass + 84 skip (new baseline: 52 pass, was 48)
- **Learnings for future iterations:**
  - Template literal backslash escaping for embedded JS: `\\\\\\n` in TS template (6 backslashes + n) → `\\\n` in HTML → `\\` + `\n` in JS = backslash + newline ✓
  - Playground uses vanilla ES5 `var` style (except async pgSend) to avoid transpilation concerns
  - Prod baseline now: 52 pass + 84 skip (SKIP_AUTH_TESTS=1)
---

## 2026-04-13 — ACP-166
- What was implemented: Core Web Vitals audit and performance optimization
- Files changed:
  - `src/pages/landing.ts` — replaced render-blocking `@import url(...)` with non-blocking font load: preconnect hints (fonts.googleapis.com + fonts.gstatic.com), preload as="style", async stylesheet (media="print" onload trick), noscript fallback. Added inline critical CSS (background-color + font-family + skip-link).
  - `src/pages/changelog.ts` — same font optimization as landing.ts
  - `test/smoke/public.spec.ts` — 3 new ACP-166 tests: landing non-blocking fonts (raw HTML check), changelog non-blocking fonts, api-reference no @import
- **Lighthouse scores (before → after):**
  - Landing: Performance 97→99, LCP 1.7s→1.6s, CLS 0.093→0.02, TBT 0ms, TTFB 20ms
  - Pricing: 100 (no change — already optimal)
  - Docs: 100 (no change — already optimal)
  - Blog: 100 (no change — already optimal)
  - Render-blocking Google Fonts @import removed: 775ms savings
- Smoke tests: staging 103 pass + pre-existing webkit; prod 56 pass + 84 skip (new baseline: 56 pass)
- **Learnings for future iterations:**
  - Playwright page.content() returns rendered DOM after JS runs — `media="print"` becomes `media="all"` after onload fires. Use `page.request.get()` for raw HTML source checks
  - Non-blocking font pattern: preconnect + preload + `media="print" onload="this.onload=null;this.media='all'"` + noscript
  - CLS improvement from 0.093 to 0.02 — the inline critical CSS with explicit font-family prevented layout shift from font swap
  - Prod baseline now: 56 pass + 84 skip (SKIP_AUTH_TESTS=1)
---

## 2026-04-13 — ACP-167
- What was implemented: Sync and deploy ACP-160-166 to GitHub
- Branch ralph/nexus pushed to git@github.com:scobb/nexus.git
- All routes verified on production:
  - /demo → 200 (Demo Dashboard with seeded trace data)
  - /changelog → 200
  - /docs/api-reference → 200 (with interactive playground)
- Prod smoke tests already passing: 56 pass + 84 skip (SKIP_AUTH_TESTS=1)
- ALL 167 STORIES COMPLETE
---
